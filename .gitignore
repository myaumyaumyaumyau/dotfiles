# === OS ===
.DS_Store
*.swp
*.swo
*~

# === Кэш / локальные данные внутри пакетов ===
**/.cache/
**/.local/
**/cache/

# === Рантайм-состояние ===
**/lazygit/state.yml

# === Yazi-флейворы — ставятся через `ya pkg install` по package.toml ===
yazi/.config/yazi/flavors/

# === Karabiner — в репо только karabiner.json; остальное создаёт сам Karabiner ===
karabiner/.config/karabiner/automatic_backups/
karabiner/.config/karabiner/assets/

# === Бэкапы, создаваемые install.sh ===
.dotfiles-backup-*/

# === Секреты — НЕ коммитить ===
.gitconfig-github
.gitconfig-local
*.env
.env
.env.*
*.secret
*.secrets
.secrets
id_rsa
id_ed25519
*.pem
*.key
